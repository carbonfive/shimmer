source "https://rubygems.org"

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in shimmer.gemspec
gemspec
gem 'chrome_remote', github: 'andrewhao/chrome_remote', branch: 'event-tweaks'

